import 'package:flutter/material.dart';

class Response extends StatelessWidget {
  int? code;
  String? message;
  Response({super.key, this.code, this.message});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}