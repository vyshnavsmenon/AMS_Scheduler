import 'package:flutter/material.dart';

class Response extends StatelessWidget {
  int? code;
  String? message;
  var data;
  Response({super.key, this.code, this.message, this.data});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}