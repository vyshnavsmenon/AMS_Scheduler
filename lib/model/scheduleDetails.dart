import 'package:flutter/material.dart';

class SchedlueDetails extends StatelessWidget {
  final String name;
  final String toName;
  final String date;
  final String time;
  final String uid;
   SchedlueDetails({
    required this.name,
    required this.toName,
    required this.date,
    required this.time,
    required this.uid,
   });

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}